import { StyleSheet } from "react-native";
import Colors from "../../../costants/Colors";
const ImageWeatherStyle = StyleSheet.create({
  screen: {
    height: 70,
    width: 70,
    color: Colors.accent,
  },
});

export default ImageWeatherStyle;
