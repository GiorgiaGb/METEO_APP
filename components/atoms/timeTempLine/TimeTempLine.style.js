import { StyleSheet } from "react-native";
import Colors from "../../../costants/Colors";

const TimeTempLineStyle = StyleSheet.create({
  screen: {
    color: Colors.accent,
    fontSize: 16,
    fontWeight: "200",
    textAlign: "center",
    paddingVertical: 31,
  },
});

export default TimeTempLineStyle;
