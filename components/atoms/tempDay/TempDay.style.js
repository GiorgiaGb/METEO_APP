import { StyleSheet } from "react-native";
import Colors from "../../../costants/Colors";

const TempDayStyle = StyleSheet.create({
  screen: {
    marginTop: 10,
    fontSize: 35,
    fontWeight: "700",
    color: Colors.accent,
  },
});

export default TempDayStyle;
