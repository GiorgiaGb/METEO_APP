import { StyleSheet } from "react-native";

const WeatherStateStyle = StyleSheet.create({
  screen: {
    fontSize: 20,
    fontWeight: "200",
    color: "white",
  },
});

export default WeatherStateStyle;
