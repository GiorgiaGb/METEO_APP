import { StyleSheet } from "react-native";
import { StatusBar } from "react-native";
import Colors from "../../../costants/Colors";

const WeatherWeekStyle = StyleSheet.create({
  container: {
    flexDirection: "row",
    marginLeft: "7",
  },
});

export default WeatherWeekStyle;
