import { StyleSheet } from "react-native";

const DotLineStyle = StyleSheet.create({
  screen: {
    flexDirection: "row",
    width: "95%",
    // justifyContent: "space-evenly",
    justifyContent: "space-between",
    position: "relative",
    top: 42,
  },
});

export default DotLineStyle;
